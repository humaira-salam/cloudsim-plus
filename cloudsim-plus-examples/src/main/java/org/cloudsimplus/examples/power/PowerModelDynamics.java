/*
package org.cloudsimplus.examples.power;

import org.cloudbus.cloudsim.utilizationmodels.UtilizationModelDynamic;

public abstract class PowerModelDynamic {
    private double maxCpuPowerPercentage;
    private  double initialCpuPowerPercent;


    //attributes

    */
/** check for intial and max power instead

     *//*

    public static void main(String[] args) {new PowerModelDynamic();}

    public PowerModelDynamic(){
        if (maxCpuPowerPercentage < initialCpuPowerPercent) {
            throw new IllegalArgumentException("Max CPU usage must be equal or greater than the initial CPU usage.");
        }

        initialCpuPowerPercent = Math.min(initialCpuPowerPercent, 1);
        maxCpuPowerPercentage = Math.min(maxCpuPowerPercentage, 1);
        //PowerModelDynamic um;

        if (initialCpuPowerPercent < maxCpuPowerPercentage) {
            //um = new PowerModelDynamic(initialCpuPowerPercent);
        } else {
            // um = new PowerModelDynamic(initialCpuPowerPercent);
        }

        //um.setMaxResourceUtilization(maxCpuPowerPercentage);

    }

}
*/
